# Unified Project Share

This repository contains today's work in one easy-to-share structure.

## Project Structure

- `project/analytical-hackathon`: Main application and APIs.
- `project/fast-pass-public`: Additional files created today.

## Notes

- Local-only artifacts were excluded for sharing (`.env*`, `node_modules`, local DB files, uploads, build outputs).
- Use the main app from `project/analytical-hackathon`.
